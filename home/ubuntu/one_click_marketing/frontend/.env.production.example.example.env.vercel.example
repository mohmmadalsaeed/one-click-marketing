# Frontend Environment Variables for Vercel (.env.production.example or set in Vercel UI)

# For Vercel deployment, the backend API will be available at the /api path on the same domain.
NEXT_PUBLIC_API_BASE_URL="/api"

# Any other public-facing environment variables needed by Next.js at runtime or build time
# Example: NEXT_PUBLIC_GOOGLE_ANALYTICS_ID="UA-XXXXX-Y"

